#include <ros/ros.h>

#include "planning/AStarPlanner.h"

int main(int argc, char **argv) {
    ros::init(argc, argv, "planning_node");
    ros::NodeHandle nh;

    while (ros::ok()) {
        
    }
    return 0;
}